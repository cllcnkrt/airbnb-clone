import { IconType } from "react-icons/lib";

export declare module ICategoryItem {
    // eslint-disable-next-line no-unused-vars
    interface Props {
        label: string;
        icon: IconType;
        selected?: boolean;
    }
}
