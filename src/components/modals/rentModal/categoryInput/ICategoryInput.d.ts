import { IconType } from "react-icons/lib";

export declare module ICategoryInput {
    // eslint-disable-next-line no-unused-vars
    interface Props {
        onClick: (value: string) => void;
        selected?: boolean;
        label: string;
        icon: IconType;
    }
}
